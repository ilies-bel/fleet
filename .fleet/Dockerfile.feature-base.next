FROM node:20-slim

WORKDIR /app
RUN apt-get update && apt-get install -y git curl && rm -rf /var/lib/apt/lists/*

# Entrypoint runs Next.js dev server with HMR
ENV NODE_ENV=development
ENV PORT=3000
EXPOSE 3000

CMD ["sh", "-c", "npm install && npm run dev -- --hostname 0.0.0.0 --port 3000"]
