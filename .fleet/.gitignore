# Auto-generated by `fleet init` — fleet per-project ignores.
#
# Committed: fleet.toml, Dockerfile.feature-base (and config/, fragments/).
# Ignored below: regenerable (`fleet add`) or machine-specific files.

# Machine-specific / secret-bearing
shared.env
host-runner.pid

# Per-feature runtime artifacts generated by `fleet add` (.fleet/<name>/...)
*/docker-compose.yml
*/feature.env
*/info.toml
*/state.json
