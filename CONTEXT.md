# Project Context

Canonical domain terms for this project. Edited via `mars glossary`.

## Language

**active feature**:
The single feature currently routed to the stable preview port (localhost:3000); at most one feature is active at a time and the gateway is its source of truth.

**feature**:
A reviewable branch running as an isolated container, shown as one card in the Fleet dashboard with its own lifecycle state and controls.
