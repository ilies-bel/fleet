FROM node:20-slim

WORKDIR /app
RUN apt-get update && apt-get install -y git curl && rm -rf /var/lib/apt/lists/*
RUN npm install -g nodemon

ENV NODE_ENV=development
EXPOSE 3001

CMD ["sh", "-c", "npm install && npx nodemon --watch src src/index.js"]
